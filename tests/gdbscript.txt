define hook-stepi
	printf "QQQW1234 0x%lx 0x%lx 0x%lx 0x%lx\n", $w1, $w2, $w3, $w4
  printf "QQQFLG   %c %c %c %c\n", ($cpsr >> 31) & 1 ? 'N' : '-', ($cpsr >> 30) & 1 ? 'Z' : '-', ($cpsr >> 29) & 1 ? 'C' : '-', ($cpsr >> 28) & 1 ? 'V' : '-'
end

b ret_test_enter

display/i $pc
set verbose off
start
c
while !endflag
stepi
end
